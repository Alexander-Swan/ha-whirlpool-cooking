set_cook:
  name: Set cook
  description: Start or update cooking on an oven cavity.
  fields:
    entity_id:
      required: true
      selector:
        entity:
          integration: whirlpool_cooking
    cavity:
      required: true
      selector:
        select:
          options:
            - upper
            - lower
    mode:
      required: true
      default: bake
      selector:
        select:
          options:
            - air_fry
            - bake
            - broil
            - convect_bake
            - convect_broil
            - convect_roast
            - keep_warm
    target_temperature_celsius:
      required: true
      selector:
        number:
          min: 0
          max: 300
          step: 1
          unit_of_measurement: "C"
set_cook_time:
  name: Set cook time
  description: Set the timed cook duration on an oven cavity after preheat.
  fields:
    entity_id:
      required: true
      selector:
        entity:
          integration: whirlpool_cooking
    cavity:
      required: true
      selector:
        select:
          options:
            - upper
            - lower
    cook_time:
      required: true
      selector:
        text:

stop_cook:
  name: Stop cook
  description: Stop cooking on an oven cavity.
  fields:
    entity_id:
      required: true
      selector:
        entity:
          integration: whirlpool_cooking
    cavity:
      required: true
      selector:
        select:
          options:
            - upper
            - lower
